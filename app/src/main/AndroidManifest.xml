<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.erd.reblood">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".app.AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.ListEventActivity"
            android:label="Event" >

        </activity>

        <activity android:name=".activity.LoginActivity"
            android:label="Login" >

        </activity>

        <activity android:name=".activity.RegisterActivity"
            android:label="Register">

        </activity>

        <activity android:name=".utils.BarcodeScanner"
            android:label="QR Code Scanner">

        </activity>

        <activity android:name=".activity.HistoryActivity"
            android:label="History">
        </activity>


        <activity android:name=".TextDrawable.TextDrawableActivity"
            android:label="List TextDrawable" >
        </activity>

        <activity android:name=".TextDrawable.ListActivity"
            android:label="List TextDrawable" >
        </activity>

    </application>

</manifest>